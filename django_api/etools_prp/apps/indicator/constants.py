class ValueType:
    # TODO: Possibly rename / make sure name is clear
    # TODO: Replace cryptic 'c', 'v', etc references in codebase with references to this file
    VALUE = 'v'
    DENOMINATOR = 'd'
    CALCULATED = 'c'

    @classmethod
    def empty(cls):
        return {

        }
