---
kind: Service
apiVersion: v1
metadata:
  name: id_management_frontend
  namespace: {{NAMESPACE}}
spec:
  ports:
  - protocol: TCP
    port: 3000
    nodePort: 30050
  type: NodePort
  selector:
    app: id_management_frontend-container
---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: id_management_frontend
  labels:
    app: id_management_frontend
  annotations:
    revision: "{{CF_SHORT_REVISION}}"
  namespace: {{NAMESPACE}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: id_management_frontend-container
  template:
    metadata:
      labels:
        app: id_management_frontend-container
    spec:
      containers:
        - name: id_management_frontend
          ports:
            - containerPort: 3000
          image: tivix/etools-prp-id-management-frontend:{{CF_SHORT_REVISION}}
          command: ["bash"]
          args: ["-c", "node node_modules/serve/bin/serve.js -s build"]
